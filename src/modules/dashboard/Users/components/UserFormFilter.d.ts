import React from "react";
import "./index.scss";
type Props = {
    closeModal: () => void;
};
declare const UserFormFilter: ({ closeModal }: Props) => React.JSX.Element;
export default UserFormFilter;
