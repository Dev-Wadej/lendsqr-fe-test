import React from "react";
type Props = {
    icon: string;
    name: string;
    amount: string;
};
export default function UserDashboardBox({ amount, icon, name }: Props): React.JSX.Element;
export {};
